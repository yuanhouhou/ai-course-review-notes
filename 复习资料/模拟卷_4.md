# 研究生人工智能公共基础分层考试模拟卷_4

> 考试时间：120 分钟　　满分：100 分  
> 范围：机器学习、深度学习、自然语言处理、语音信号处理、计算机视觉。  
> 说明：基础简答题 40 分，前沿简答题 40 分，编程题 20 分。

---

## 一、基础简答题（共 40 分）

### 1. 三类机器学习范式（12 分）

**题目描述：**比较监督学习、无监督学习和强化学习的训练信号、目标及典型任务。为“垃圾邮件识别、用户分群、仓储机器人避障”分别选择一种范式并说明理由。

**答题要求：**给出输入/反馈形式及一个容易混淆点；建议作答字数 200～300 字。

### 2. 图像任务与视觉模型（14 分）

**题目描述：**比较图像分类、目标检测和语义/实例分割的输出与适用任务。说明 CNN 利用图像局部空间结构的方式，并简述视觉 Transformer 与 CNN 的一项差异。

**答题要求：**结合“缺陷判别、车辆定位、医学病灶勾画”三类场景作答；建议作答字数 220～320 字。

### 3. NLP 表示与语言模型演进（14 分）

**题目描述：**比较独热表示、静态词向量和语境化动态表示；说明 N-gram、RNN/Transformer 语言建模的主要差异，以及 Transformer 为什么适合建模较长上下文。

**答题要求：**说明至少一个传统方法局限；建议作答字数 250～350 字。

---

## 二、前沿简答题（共 40 分）

### 4. 大语言模型对齐与能力边界（12 分）

**题目描述：**说明预训练、指令微调和偏好对齐各自解决什么问题。为什么“回答流畅”不能作为“回答可信”的充分条件？

**答题要求：**结合幻觉、证据核验和拒答边界作答；建议作答字数 220～320 字。

### 5. 语音交互系统的端到端评估（14 分）

**题目描述：**为校园语音问答系统设计“语音输入→识别/理解→检索或生成→语音输出”的处理链，并给出至少四项评测指标。说明噪声、口音和隐私会带来哪些挑战。

**答题要求：**兼顾准确性、实时性、可用性和安全；建议作答字数 250～350 字。

### 6. 多模态视觉生成的风险控制（14 分）

**题目描述：**某宣传系统可根据文字、参考图和产品资料生成海报。分析其价值与至少三类风险，并设计发布前后的控制措施。

**答题要求：**可从版权、事实一致性、偏见、误导内容、敏感数据和人工审核作答；建议作答字数 250～350 字。

---

## 三、编程改错题（10 分）

### 7. 多分类损失与标签改错

```python
logits = model(images)                 # [batch_size, 10]
probs = torch.softmax(logits, dim=1)
labels = labels.float()
loss = nn.CrossEntropyLoss()(probs, labels)
loss.backward()
```

**请回答：**上述代码至少有哪两处错误？给出适用于 10 分类整数标签的正确关键代码，并解释 `CrossEntropyLoss` 的输入约定。

---

## 四、编程理解题（10 分）

### 8. CNN 尺寸、Dropout 与推理

输入为 `[B, 3, 32, 32]`，网络为：`Conv2d(3,8,3,padding=1) → ReLU → MaxPool2d(2) → Conv2d(8,16,3,padding=1) → ReLU → MaxPool2d(2) → Flatten → Dropout(0.5) → Linear`。

**请回答：**

1. 两次池化后的张量形状及 `Linear` 输入特征数；
2. Dropout 训练和推理时的差异；
3. 对单张图片推理前为什么应执行 `model.eval()` 与 `torch.no_grad()`？

